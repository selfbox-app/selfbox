// The canonical URL of Selfbox Cloud. Hard-coded so the "Sign in with
// Selfbox" default path doesn't depend on any user-typed input — that's
// the trust anchor that makes the Cloud-first sign-in flow safe against
// typos and phishing look-alikes.
export const SELFBOX_CLOUD_URL = "https://selfbox.app";
