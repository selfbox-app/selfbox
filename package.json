{
  "name": "selfbox",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test:unit": "pnpm --filter=@selfbox/web test:unit",
    "test:integration": "pnpm --filter=@selfbox/web test:integration",
    "test:e2e": "playwright test",
    "db:generate": "turbo db:generate --filter=@selfbox/database",
    "db:migrate": "turbo db:migrate --filter=@selfbox/database",
    "db:seed": "turbo db:seed --filter=@selfbox/database",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,css,md}\""
  },
  "devDependencies": {
    "@playwright/test": "^1.59.0",
    "portless": "^0.10.1",
    "prettier": "^3.8.1",
    "turbo": "^2.8.17",
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  }
}
